# This is a basic workflow to help you get started with Actions

name: Add repo to Infracost app

# Controls when the workflow will run
on: [create, workflow_dispatch]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      REPO_ID: ${{ github.event.repository.id }}
      APP_ID: ${{ secrets.INFRACOST_APP_ID }}
      PAT: ${{ secrets.REPO_PAT }}
    steps:
      - name: Add repo to Infracost App
        run: |
            curl -L \
              -X PUT \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: Bearer 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" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              https://api.github.com/user/installations/51281931/repositories/${{ github.event.repository.id }}
